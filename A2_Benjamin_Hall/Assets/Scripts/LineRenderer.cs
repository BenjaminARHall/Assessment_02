using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LineRenderer : MonoBehaviour {
    //public LineRenderer rope;

    //public float counter;

    //public float distance;

    //public Transform origin;
    //public Transform tetherPoint;

    //public float lineDrawSpeed = 6f;
    //// Use this for initialization
    //void Start()
    //{
    //    rope = GetComponent<LineRenderer>();
    //   // rope.SetPosition(0, origin.position);
    //    //rope.SetWidth(.45f, .45f);

    //    distance = Vector3.Distance(origin.position, tetherPoint.position);

    //}

    //// Update is called once per frame
    //void Update()
    //{

    //    if (counter < distance)
    //    {
    //        counter += .1f / lineDrawSpeed;

    //        float x = Mathf.Lerp(0, distance, counter);

    //        Vector3.pointA = origin.position;
    //        Vector3.pointB = tetherPoint.position;

    //        Vector3 pointAlongLine = x * Vector3.Normalize(pointB - pointA) + pointAlongLine;
    //        rope.SetPosition(1, pointAlongLine);
    //    }

    //}
}
